Riff 0.5.3 repairs the sound compass, gives the living form calmer musical motion, and lets AI writers develop a complete generation recipe.

- Compass clicks and drags follow the visible point at desktop and phone widths. Touch, keyboard input and dragging outside the pad work consistently. The point remains responsive while writing; only the latest released direction runs next, and draft edits take precedence. Notifications no longer obstruct the pad.
- The living form carries broader musical gestures with a slower release. Independent fast ripples are removed, waveform detail stays within the folds, and seeking and MP4 export use the same deterministic audio response. Movement, color and texture remain adjustable; the corner riff wordmark is retained.
- Local AI and OpenRouter writers receive the current score, planning, sampling, acoustic settings and source listening context. They return an editable generation recipe using the same contract as producer review. Automatic songwriting carries those settings into the queued take. Holds, draft Undo and writing provenance survive the round trip.
- Includes the 0.5.2 Direct/study generation repairs, internal mineral color and texture controls, and 4K/60 fps MP4 export.

Validation includes 97 application tests, the complete browser suite, actual local and Gemini 3.8 Flash writing, and a Gemini-written recipe rendered through the native engine. Visual comparisons rejected an overly still first candidate before restoring broad musical motion. The native engine is unchanged from 0.5.1. Release CI checks packaged installation and Metal, CPU and CUDA compilation; NVIDIA inference remains unverified.
