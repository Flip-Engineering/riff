OpenRouter songwriting and score editing now use the selected model’s normal output allowance. The small local writer’s token limit no longer cuts cloud drafts off before completion.

Both cloud writing flows request structured, complete results. Local writer controls remain available when that writer is selected, and failed or cancelled requests preserve the current draft.
